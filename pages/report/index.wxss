/* report.wxss - 报告页样式 */

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
  margin-bottom: 24rpx;
}

/* 统计卡片网格 */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.stat-box {
  text-align: center;
  padding: 24rpx 16rpx;
  background: #f9fafb;
  border-radius: 16rpx;
}

.stat-num {
  font-size: 48rpx;
  font-weight: 800;
  line-height: 1.2;
}

.stat-label {
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
}

/* 图表 */
.chart-wrap {
  margin-top: 16rpx;
}

.chart-bars {
  display: flex;
  align-items: flex-end;
  gap: 6rpx;
  height: 160rpx;
  padding: 0 8rpx;
}

.bar-item {
  flex: 1;
  height: 100%;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.bar-fill {
  width: 100%;
  max-width: 24rpx;
  border-radius: 4rpx 4rpx 0 0;
  min-height: 4rpx;
  transition: height 0.3s;
}

.bar-fill.correct {
  background: #10b981;
}
.bar-fill.wrong {
  background: #ef4444;
}

.chart-legend {
  display: flex;
  justify-content: center;
  gap: 32rpx;
  margin-top: 16rpx;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 22rpx;
  color: #999;
}

.legend-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 4rpx;
}

.legend-dot.correct {
  background: #10b981;
}
.legend-dot.wrong {
  background: #ef4444;
}

/* 常见错误标签 */
.mistakes-list {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.mistake-tag {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  padding: 10rpx 20rpx;
  background: #fef2f2;
  border: 1rpx solid #fecaca;
  border-radius: 32rpx;
  font-size: 24rpx;
  color: #dc2626;
}

.mistake-count {
  font-size: 20rpx;
  color: #999;
}

.btn-go-mistakes {
  width: 100%;
  padding: 20rpx;
  background: #f9fafb;
  border: 1rpx solid #e5e7eb;
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #333;
  text-align: center;
}

.btn-go-mistakes::after {
  border: none;
}

/* IRT 能力值卡片 */
.theta-card {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24rpx;
  padding: 32rpx;
  margin: 0 auto;
  background: linear-gradient(135deg, #eff6ff, #e0e7ff);
  border-radius: 24rpx;
  border: 2rpx solid #bfdbfe;
  max-width: 480rpx;
}

.theta-title {
  font-size: 28rpx;
  color: #6b7280;
}

.theta-number {
  font-size: 64rpx;
  font-weight: 900;
  color: #2563eb;
  line-height: 1;
}

.theta-grade {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  font-weight: 900;
  color: #fff;
}

.theta-grade.gold {
  background: linear-gradient(135deg, #f59e0b, #d97706);
  box-shadow: 0 4rpx 16rpx rgba(245, 158, 11, 0.4);
}

.theta-grade.silver {
  background: linear-gradient(135deg, #6b7280, #4b5563);
  box-shadow: 0 4rpx 16rpx rgba(107, 114, 128, 0.4);
}

.theta-grade.bronze {
  background: linear-gradient(135deg, #d97706, #b45309);
  box-shadow: 0 4rpx 16rpx rgba(217, 119, 6, 0.4);
}

.theta-tip {
  font-size: 24rpx;
  color: #6b7280;
}
